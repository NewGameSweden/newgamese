<%= form_for @article do |f| %>
	<% if @article.errors.any? %>
		<div id="error_explanation">
			<h5>
				<%= pluralize(@article.errors.count, "error") %> förhindrade denna nyhet från att sparas:
			</h5>
			<ul>
				<% @article.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<p>
		<%= f.label :title %><br>
		<%= f.text_field :title %>
	</p>

	<p>
		<%= f.label :text %><br>
		<%= f.text_area :text %>
	</p>
 
  	<p>
    	<%= f.submit 'Submit', :class => 'waves-effect waves-lighten btn' %>
  	</p>
<% end %>
