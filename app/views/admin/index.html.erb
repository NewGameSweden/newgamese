<% content_for :title do "Admin panel" end %>

<br>
<br>
<br>

<%= form_tag "/admin/handle_admin", :method => :post, :multipart => true do %>
<%= select_tag "username", options_for_select(@users.all.order(:username).collect { |user| user.username  }), {:include_blank => "Användare"} %>
    <%= button_tag :class => "btn-large btn-primary", :name => "add" do %> Lägg till<% end %>
    <%= button_tag :class => "btn-large btn-primary" do %> Ta bort<% end %>
<% end %>
